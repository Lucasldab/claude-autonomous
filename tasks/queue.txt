# Autonomous task queue. Format: <project-dir>|<prompt>
# Runner picks the TOP non-comment, non-blank line.
#
# First-run rules baked into prompts:
# - Use a feature branch named autonomous/<short-task-slug>
# - Never touch uncommitted changes already in the working tree (stash with note in PR if blocking)
# - Open a draft PR; do not merge
# - If you cannot proceed without making destructive changes, write a state note explaining and exit cleanly

local-u-llm|Python scripts for local Ollama. Read chat.py, model-test.py, searching.py, Modelfile. Write README.md (what each script does, how to run, model assumed) AND IMPROVEMENTS.md (top 3 quality-of-life additions ranked by effort/impact). Two files, one PR.
rfinances|Rust personal finance tool. Read src/ and Cargo.toml. There is a file named '~' in repo root which is suspicious — investigate (is it accidentally created from `git stash` typo or `vim ~`?). Write SCAN.md: (a) what '~' contains and what to do with it (delete? gitignore? rename?), (b) modules + responsibilities, (c) one missing test. Do NOT delete '~' — only document. Open draft PR with SCAN.md.
mtgspoilers|Add a CONTRIBUTING.md if missing. Concise: dev setup, test command, how to add a new spoiler source (the 4-tier system extension point). Open draft PR.
enderite|Empty repo. Scaffold a Fabric mod for Minecraft Java targeting the latest stable MC + Fabric Loom. Add: build.gradle(.kts), settings.gradle, gradle.properties (with MC/Fabric/Yarn/Loom versions documented at top of file), src/main/resources/fabric.mod.json (id=enderite, name=Enderite, description='Adds an enderite tier stronger than netherite', author=Lucasldab), src/main/java/com/lucasldab/enderite/Enderite.java (ModInitializer with logger), src/client/.gitkeep, README.md (what it is, how to build, gradle runClient instructions), .gitignore (gradle + IDE). Use the smithing-template upgrade flow like netherite (decision documented in README). Open draft PR titled 'feat: scaffold Fabric mod'.
enderite|Add the enderite material chain (depends on the scaffold PR being merged or branched off). Read existing scaffold. Add: EnderiteItems.java registering RAW_ENDERITE, ENDERITE_INGOT, ENDERITE_UPGRADE_SMITHING_TEMPLATE; EnderiteBlocks.java registering ENDERITE_BLOCK, RAW_ENDERITE_BLOCK, ENDERITE_ORE (End dim), DEEPSLATE_ENDERITE_ORE (overworld optional rare), END_ENDERITE_ORE (end_stone variant). Smelting recipes (raw → ingot in furnace AND blast furnace, faster than netherite). Loot tables for ores. Lang en_us.json. Tags (mineable/pickaxe with diamond+ requirement, items/enderite_ingots, blocks/enderite_blocks). Make the design call on End vs Nether spawn; document in README. DO NOT generate or commit any PNGs. Create a TEXTURES.md at repo root listing every texture path expected by item/block models (assets/enderite/textures/item/*.png and assets/enderite/textures/block/*.png) so Lucas can supply art later — Minecraft renders the pink/black missing-texture fallback in the meantime. Open draft PR titled 'feat: enderite material (ore + ingot + blocks)'.
enderite|Add enderite tools (sword, pickaxe, axe, shovel, hoe). Stats: every numeric > netherite — durability, attack damage, mining speed; mining level = netherite (or higher if MC version uses tags). Smithing recipe: netherite_X + ENDERITE_INGOT + ENDERITE_UPGRADE_SMITHING_TEMPLATE → enderite_X. Recipes JSON, lang entries, item models pointing to texture paths under assets/enderite/textures/item/ — DO NOT generate or commit any PNGs. Instead append a TEXTURES.md at repo root listing every texture path the mod expects (e.g., assets/enderite/textures/item/enderite_sword.png) so Lucas (or another contributor) can drop in art later. Minecraft will render the missing-texture pink/black checker until art is supplied — that is the intended placeholder. Open draft PR titled 'feat: enderite tools'.
enderite|Add enderite armor (helmet, chestplate, leggings, boots). Stats: armor + toughness + knockback resist > netherite. Smithing recipes from netherite_armor. Equipment slot tags. Lang entries. DO NOT generate or commit any PNGs (item textures or armor layer textures). Append the missing texture paths to TEXTURES.md from the previous PR. Minecraft's missing-texture fallback is acceptable. Open draft PR titled 'feat: enderite armor'.
enderite|Add enderite world generation. Configured features + placed features for END_ENDERITE_ORE (End outer islands, rare). Optional: small chance in End city loot tables. Datapack JSON under data/enderite/worldgen/. Verify gradle build passes. Open draft PR titled 'feat: enderite worldgen + loot integration'.
