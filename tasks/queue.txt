# Autonomous task queue. Format: <project-dir>|<prompt>
# Runner picks the TOP non-comment, non-blank line.
#
# First-run rules baked into prompts:
# - Use a feature branch named autonomous/<short-task-slug>
# - Never touch uncommitted changes already in the working tree (stash with note in PR if blocking)
# - Open a draft PR; do not merge
# - If you cannot proceed without making destructive changes, write a state note explaining and exit cleanly

nocturne|Read the pin-cycle script and identify race conditions when Syncthing is mid-sync (memory note flags phone auto-pull as flaky, manual rescan workaround). Write RACES.md at repo root: each race with file:line, repro steps, suggested guard (lock file? mtime check? .syncthing.tmp marker?). Open a draft PR with RACES.md only.
smartAchievements|There are uncommitted changes in working tree — DO NOT touch them. Read .planning/ to understand current milestone, then write PLANNING-AUDIT.md summarizing: (a) which phase is in flight, (b) what is blocking the dirty changes from committing, (c) next concrete step. Open draft PR with PLANNING-AUDIT.md only. If unsure about the dirty changes' intent, document that as a blocker for Lucas in the PR description.
mineltui|There are uncommitted changes in working tree — DO NOT touch them. Read .planning/ to understand current milestone. Write PLANNING-AUDIT.md: phase status + blocker for dirty changes + next step. Open draft PR with PLANNING-AUDIT.md only.
icloud-tools|Audit lsb_crypt.py (two-factor PNG-LSB + password encryption). Write SECURITY-NOTES.md: any crypto smells (constant-time compare, IV reuse, password→key derivation choice, error-message leaks), severity per finding, no fixes — just findings. Open draft PR with SECURITY-NOTES.md.
icloud-migrate|Read README and any scripts. Memory says: photos pulling, docs encrypted at ~/backup/, notes manual; pending Syncthing + photo triage. Write STATUS.md: what's done, what's pending (with concrete next step for each), what cannot proceed without Lucas. Open draft PR with STATUS.md.
crypt-files|Audit for crypto correctness. Same shape as icloud-tools task: SECURITY-NOTES.md with findings only. Open draft PR.
mrm|Rust TUI manhwa manager. Read crates/, scraper/, Cargo.toml. Write SCAN.md at repo root listing: (a) modules and their responsibilities, (b) any TODO/FIXME comments with file:line, (c) one risky area (scraper rate-limiting? notification backoff? state file corruption on crash?) with suggested test. Open draft PR with SCAN.md only.
mtgspoilers|Rust TUI MTG spoilers tool. Read src/ and migrations/. Write SCAN.md: (a) modules + responsibilities, (b) the 4-tier confidence system flow with file:line, (c) one edge case in dedup (image hash collisions? near-duplicates? source disagreements?) with proposed test. Open draft PR with SCAN.md only.
noguisteam|Rust TUI Steam manager via SteamCMD. Read src/. Write SCAN.md: (a) modules + responsibilities, (b) shell injection risk surface around SteamCMD invocation with file:line, (c) one concrete sanitization-test you would add. Open draft PR with SCAN.md only.
rkanban|Rust TUI Kanban. Read src/ and work.json. Write SCAN.md: (a) modules + responsibilities, (b) data-loss risk on concurrent edits or crash mid-write with file:line, (c) one atomic-write or schema-version test you would add. Open draft PR with SCAN.md only.
tuiclicker|Go TUI clicker. Read cmd/ and internal/. Write SCAN.md: (a) packages + responsibilities, (b) any race conditions in tick/render goroutines with file:line, (c) one concrete -race test you would add. Open draft PR with SCAN.md only.
protONsteam|Browser extension showing ProtonDB ratings on Steam pages. Read content.js, background.js, manifest.json. Write SCAN.md: (a) message-passing surface and CSP impact, (b) any innerHTML/textContent injection risk on Steam-controlled DOM with file:line, (c) one concrete XSS-defense test. Open draft PR with SCAN.md only.
rhonetyping|Rust project with no README. Read Cargo.toml and src/. Write README.md (concise: what it is, how to build, how to run) AND PLANNING.md (current state assessment + next obvious milestone). Two files, one PR.
local-u-llm|Python scripts for local Ollama. Read chat.py, model-test.py, searching.py, Modelfile. Write README.md (what each script does, how to run, model assumed) AND IMPROVEMENTS.md (top 3 quality-of-life additions ranked by effort/impact). Two files, one PR.
rfinances|Rust personal finance tool. Read src/ and Cargo.toml. There is a file named '~' in repo root which is suspicious — investigate (is it accidentally created from `git stash` typo or `vim ~`?). Write SCAN.md: (a) what '~' contains and what to do with it (delete? gitignore? rename?), (b) modules + responsibilities, (c) one missing test. Do NOT delete '~' — only document. Open draft PR with SCAN.md.
mtgspoilers|Add a CONTRIBUTING.md if missing. Concise: dev setup, test command, how to add a new spoiler source (the 4-tier system extension point). Open draft PR.
