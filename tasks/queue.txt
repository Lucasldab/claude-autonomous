# Autonomous task queue. Format: <project-dir>|<prompt>
# Runner picks the TOP non-comment, non-blank line.
#
# First-run rules baked into prompts:
# - Use a feature branch named autonomous/<short-task-slug>
# - Never touch uncommitted changes already in the working tree (stash with note in PR if blocking)
# - Open a draft PR; do not merge
# - If you cannot proceed without making destructive changes, write a state note explaining and exit cleanly

enderite|Add enderite tools (sword, pickaxe, axe, shovel, hoe). Stats: every numeric > netherite — durability, attack damage, mining speed; mining level = netherite (or higher if MC version uses tags). Smithing recipe: netherite_X + ENDERITE_INGOT + ENDERITE_UPGRADE_SMITHING_TEMPLATE → enderite_X. Recipes JSON, lang entries, item models pointing to texture paths under assets/enderite/textures/item/ — DO NOT generate or commit any PNGs. Instead append a TEXTURES.md at repo root listing every texture path the mod expects (e.g., assets/enderite/textures/item/enderite_sword.png) so Lucas (or another contributor) can drop in art later. Minecraft will render the missing-texture pink/black checker until art is supplied — that is the intended placeholder. Open draft PR titled 'feat: enderite tools'.
enderite|Add enderite armor (helmet, chestplate, leggings, boots). Stats: armor + toughness + knockback resist > netherite. Smithing recipes from netherite_armor. Equipment slot tags. Lang entries. DO NOT generate or commit any PNGs (item textures or armor layer textures). Append the missing texture paths to TEXTURES.md from the previous PR. Minecraft's missing-texture fallback is acceptable. Open draft PR titled 'feat: enderite armor'.
enderite|Add enderite world generation. Configured features + placed features for END_ENDERITE_ORE (End outer islands, rare). Optional: small chance in End city loot tables. Datapack JSON under data/enderite/worldgen/. Verify gradle build passes. Open draft PR titled 'feat: enderite worldgen + loot integration'.
